# Workspace 

作業用ディレクトリ

## Directory Structure

```text
.
├── README.md
├── .gitignore
├── data/                           <- データ置き場
├── docker/                         <- Dockerfile/docker-compose.yml/pyproject.toml/poetry.lock置き場
├── notebooks/                      <- 実験用notebook置き場
├── scripts/                        <- 実行用script置き場
└── utils/                          <- 外部API呼び出しの直接的な関数置き場
```